function atividadeJs() {
    estrutura = 1;
    soma = 0;

    while (estrutura <= 100){
        soma = soma + estrutura;
        estrutura++;
        console.log(soma);
    }
    console.log(soma);
}